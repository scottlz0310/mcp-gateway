pre-commit:
  parallel: true
  commands:
    vet:
      run: go vet ./...

pre-push:
  parallel: true
  commands:
    lint:
      run: golangci-lint run ./...
    test:
      run: go test -race ./...
    build:
      run: go build ./cmd/server
